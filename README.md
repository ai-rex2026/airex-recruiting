# AI-Rex Recruiting

広告リクルーティング業務（ランキング／比較メディアへの掲載獲得）を、
**AIが下書きし、人は承認するだけ**で回すオペレーション基盤。

要件定義書「広告リクルーティング業務 AI化アプリ 要件定義書 v0.1」に対応する実装です。

## 構成

- Next.js 16（App Router / Server Actions） / TypeScript / Tailwind CSS v4
- Supabase（PostgreSQL / Auth / RLS）
- Claude API（メディア判定・掲載判定・競合抽出・文面生成・返信分類・URL候補提案・Web検索による自動収集）
- Vercel

## 主な画面

`/start`（案件・収集）が**標準の入口**です。ルート `/` およびログイン後は `/start` に遷移します。

**現在のUIは調査フェーズ（収集 → 陣取りボード → メディア台帳 → CSV）のみを露出しています。**
打診・承認・送信・返信・掲載のワークフロー（承認キュー／例外対応／送信ログ／返信・交渉／掲載・報告／報告書、および業務フローバー）は
実装済みですが、打診フェーズを始めるまでナビゲーション・画面から非表示にしています（各ルートはURL直アクセスでは従来どおり動作します）。

**サイドバーは2項目＋管理**（案件・収集／メディア台帳＋「管理」グループ：文面テンプレ・設定・監査ログ）です。
案件ごとの画面は**案件詳細ハブ `/campaigns/[id]`** のタブ（収集状況／陣取りボード／案件情報・KW）から辿ります。
貼り付け収集（`/collect`）は案件詳細「収集状況」タブの各KW行の「貼り付けで取り込む」から使います。
`/start` は**サマリー専用**（案件カードのクリックで案件詳細へ。収集の実行・再開・再収集などの操作は案件詳細側）、
陣取りボードは**閲覧専用**（検索順位×掲載枠×自社掲載の調査ビュー）です。
`/dashboard` は廃止し `/start` へリダイレクトします。

| パス | 役割 |
|---|---|
| `/start` | **案件・収集（標準の入口・サマリー専用）**。かんたん開始ウィザード＋案件ごとの収集状況サマリーカード（操作は案件詳細の収集状況タブ） |
| `/campaigns/[id]` | **案件詳細ハブ**。タブ：収集状況／陣取りボード／案件情報・KW |
| `/board` | 陣取りボード（**閲覧専用**）。**KW×メディア×1位〜5位の固定枠グリッド**（自社の陣は背景色・横スクロール）＋検索結果に紐づかない候補メディア。**スポンサー広告は別セクション／別シート**。CSVダウンロード対応 |
| `/collect` | 収集（業務①）。検索結果を貼り付け → AI判定 → 陣取り表 |
| `/media` `/media/[id]` | メディア台帳。名寄せ・統合・営業お断り・接触履歴 |
| `/templates` | 文面テンプレート（変数差し込み） |
| `/settings` | 送信モード・レート上限・ASPマスタ・差出人名義／署名 |
| `/audit` | 監査ログ |
| `/queue` | （非表示・URL直アクセスのみ）承認キュー。3タブ：承認待ち／例外対応／送信ログ |
| `/outbox` `/exceptions` `/replies` `/placements` | （非表示・URL直アクセスのみ）送信ログ／例外対応／返信・交渉／掲載・報告 |
| `/reports/[campaignId]` | （非表示・URL直アクセスのみ）クライアント向け報告書（印刷／PDF保存） |

## キーワード（検索ボリューム・KW候補）

キーワードは**件数の上限なし**で登録できます（案件詳細の「案件情報・KW」タブ、およびウィザードの手入力）。
1KWにつき収集1回ぶんのAPI料金がかかるため、どこまで広げるかは運用側の判断です。

- **検索ボリューム** — DataForSEO（Google 広告のキーワードプランナー由来）の月間平均検索数を KW ごとに保持します。
  登録時に自動取得し、以後は「検索ボリュームを取り直す」で更新できます。データが無い語は `—`（0件とは区別）
- **KW候補** — 「KW候補を出す」で、①AIが商材から考えた語 と ②登録済みKWをシードにした関連キーワード（実ボリューム付き）を
  `keyword_suggestions` に候補として積みます。**候補は自動では収集対象になりません**。人がチェックして「追加」した分だけ
  `keywords` に入ります（マスタの書き換えは承認制、という原則に合わせています）

## 収集センター（/start）

商品のURL、または商品情報・キーワードのテキストを入れるだけで、
関連する比較サイト・ランキングサイト・おすすめサイトが自動で集まり、陣取り表が出来上がります。

1. **商品を教えてください** — LP URL または商品説明テキストを入力。AIがLPを読み取り、案件情報と検索キーワード（8〜12個）を提案
2. **案件とキーワードの確認** — 提案内容を人が修正・キーワードを取捨選択して案件を作成
3. **自動収集（2段階）** — キーワードごとに ①Claude API の **Web検索ツール**（`web_search`）で検索上位のランキング／比較／おすすめ記事を発見し、②**各記事の本文をサーバー側で取得**して掲載枠（記事内の商品・サービスと順位、自社掲載の有無）を本文から抽出。`/collect` の貼り付け方式と同じ形（serp_snapshot → serp_entries → article_listings → outreach_targets）で陣取り表に反映。既存スナップショットは陣取り表の「記事本文から読み直す」で再抽出できます

収集は `collection_jobs` テーブルでジョブとして管理されます。ウィザードで案件を作成すると
**案件詳細の「収集状況」タブへ遷移し、そこで各KWのジョブが順次自動実行**されます（/start ではジョブを実行しません）。
/start には案件ごとの**サマリーカード**（✅完了／⏳実行中／◻未実行／❌失敗の集計＋KW数）だけが表示され、
「未実行を再開」「全KWを再収集」「（KW単位の）収集／再収集」は案件詳細の収集状況タブから行います。
ブラウザを閉じても状態は残り、完了済みKWの再収集は新しいジョブ・新しいスナップショットとして履歴に積まれます（定点観測）。

- SERP取得は **DataForSEO の SERP API（Google／日本・日本語・デスクトップ）** を使用。オーガニック（青枠）と**スポンサー広告（赤枠）を `serp_entries.result_type` で区別して保存**します。`DATAFORSEO_*` 未設定時のみ Claude API の Web検索ツールにフォールバックします（この場合は広告枠が取れず、全件オーガニック扱い・順位も厳密ではありません）
- 収集アクションの実行時間上限は **180秒**（`vercel.json` の functions maxDuration＝有料プラン前提。収集を実行するセグメントの `maxDuration` も 180）。さらにアクション内に約120秒のウォッチドッグがあり、超過時はジョブに失敗（「検索がタイムアウトしました」）を記録して「再収集」で再試行できます。running のまま3分を超えたジョブは中断とみなして引き継ぎます
- 自動検索にはAPI利用料がかかります（1キーワードあたり数円〜十数円）
- 収集まで自動、**打診対象の確定・送信の承認は人が行います**（自動送信・自動承認はしません）

## 絶対に守る原則（コードで担保）

1. **二重送信しない** — 成功／不確定／営業お断り／手動送信済 の履歴がある打診は送信対象にしない（承認時・ワーカー取得時の両方でチェック）
2. **CAPTCHAは突破しない** — 例外対応キューへ退避
3. **営業お断りは尊重** — メディア単位でフラグを立て、以後の打診から自動除外。解除は管理者のみ
4. **マスタの書き換えは承認制** — AIのURL候補は「未検証」としてメモに残すだけで、台帳への反映は人が行う

## 環境変数

```
NEXT_PUBLIC_APP_URL
NEXT_PUBLIC_SUPABASE_URL
NEXT_PUBLIC_SUPABASE_ANON_KEY
ANTHROPIC_API_KEY
DATAFORSEO_LOGIN     # SERP（スポンサー広告＋オーガニック）・検索ボリューム・関連KW
DATAFORSEO_PASSWORD
WORKER_TOKEN      # 送信ワーカー連携（任意）
WORKER_EMAIL      # ワーカー用アカウント
WORKER_PASSWORD
```

`DATAFORSEO_*` が未設定でもアプリは動きますが、**スポンサー広告の取得と検索ボリュームは無効**になります
（収集は Claude の Web検索ツールにフォールバックし、結果はすべてオーガニック扱い。KW候補はAIの提案のみ）。

## 送信ワーカー連携

送信そのものは外部の Playwright ワーカー（`form-outreach-runner`）が実行します。

```
GET  /api/worker/pull      Header: x-worker-token: <WORKER_TOKEN>
  → {"count":n,"items":[{"attempt_id","channel","to","media","domain","subject","body"}]}

POST /api/worker/report    Header: x-worker-token: <WORKER_TOKEN>
  Body: {"attempt_id":"...","result":"success|uncertain|form_error|url_error|captcha|no_solicitation",
         "detail":"...","evidence_path":"..."}
```

`result` の書き戻しは1回だけ受け付けます（2回目は 409）。

## 未実装・今後（要件定義書の未決事項に対応）

- ~~SERP 取得 API 連携（O-1）~~ — DataForSEO で対応済み（未設定時は Claude の Web検索へフォールバック）
- メール送受信基盤の統合（返信は現在、本文の貼り付けで取り込み）
- 掲載記事のスクリーンショット自動取得
- マルチテナント運用（スキーマは `tenant_id` 前提で設計済み、RLS 適用済み）

## セットアップ（別環境に立てる場合）

1. Supabase プロジェクトを作成し、`supabase/schema.sql` を適用
2. 環境変数を設定
3. `npm install && npm run build`
